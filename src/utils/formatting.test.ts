import { assertEquals } from "https://deno.land/std@0.217.0/assert/mod.ts";
import { formatMessage } from "./formatting.ts";

Deno.test("Тест экранирования специальных символов", () => {
  const testCases = [
    {
      input: "Текст с точкой.",
      expected: "Текст с точкой\\.",
      description: "Простой текст с точкой",
    },
    {
      input: "Текст со [скобками]",
      expected: "Текст со \\[скобками\\]",
      description: "Текст со скобками",
    },
    {
      input: "Текст с *звездочкой*",
      expected: "Текст с *звездочкой*",
      description: "Текст с форматированием",
    },
    {
      input: "Текст с точками...",
      expected: "Текст с точками\\.\\.\\.",
      description: "Текст с многоточием",
    },
    {
      input: "Текст с дефисом - и точкой.",
      expected: "Текст с дефисом \\- и точкой\\.",
      description: "Текст с дефисом и точкой",
    },
  ];

  for (const { input, expected, description } of testCases) {
    const result = formatMessage(input);
    assertEquals(
      result,
      expected,
      `${description} не прошел: ${result} !== ${expected}`
    );
  }
});

Deno.test("Тест форматирования HTML в Markdown", () => {
  const testCases = [
    {
      input: "<b>Жирный текст</b>",
      expected: "*Жирный текст*",
      description: "Жирный текст",
    },
    {
      input: "<i>Курсив</i>",
      expected: "_Курсив_",
      description: "Курсив",
    },
    {
      input: "<code>console.log()</code>",
      expected: "`console.log()`",
      description: "Инлайн код",
    },
    {
      input: "<b>Заголовок</b> - описание",
      expected: "*Заголовок* \\- описание",
      description: "Форматирование с дефисом",
    },
  ];

  for (const { input, expected, description } of testCases) {
    const result = formatMessage(input);
    assertEquals(
      result,
      expected,
      `${description} не прошел: ${result} !== ${expected}`
    );
  }
});

Deno.test("Тест блоков кода", () => {
  const input =
    "<pre><code>function test() {\n  console.log('test');\n}</code></pre>";
  const expected = "```\nfunction test() {\n  console.log('test');\n}\n```";
  const result = formatMessage(input);
  assertEquals(result, expected, "Блок кода не прошел");
});

Deno.test("Тест сложного форматирования", () => {
  const testCases = [
    {
      input: "📌 <b>Основные выводы</b>",
      expected: "📌 *Основные выводы*",
      description: "Эмодзи с жирным текстом",
    },
    {
      input: "<b>Что улучшить:</b> • <i>Пример</i>",
      expected: "*Что улучшить:* • _Пример_",
      description: "Смешанное форматирование",
    },
    {
      input: "• <code>test()</code> - <i>описание</i>",
      expected: "• `test()` \\- _описание_",
      description: "Список с кодом и курсивом",
    },
    {
      input: "React и JavaScript",
      expected: "`React` и `JavaScript`",
      description: "Автоматическое форматирование технических терминов",
    },
  ];

  for (const { input, expected, description } of testCases) {
    const result = formatMessage(input);
    assertEquals(
      result,
      expected,
      `${description} не прошел: ${result} !== ${expected}`
    );
  }
});

Deno.test("Тест реального примера из чата", () => {
  const input = `📌 <b>Основные выводы</b>
<i>К сожалению, ответ не соответствует вопросу и содержит некорректные выражения. Важно уметь корректно формулировать свои мысли и поддерживать профессиональную коммуникацию.</i>

<b>👍 Сильные стороны</b>
• Отсутствуют: <i>В данной ситуации сложно оценить сильные стороны, так как ответ не содержит никакой технической информации.</i>

<b>🚀 Возможности роста</b>
<b>Что улучшить:</b>
• <b>Эмоциональная реакция</b> - <i>Может негативно повлиять на рабочие отношения и процесс коммуникации.</i>`;

  const expected = `📌 *Основные выводы*
_К сожалению, ответ не соответствует вопросу и содержит некорректные выражения\\. Важно уметь корректно формулировать свои мысли и поддерживать профессиональную коммуникацию\\._

*👍 Сильные стороны*
• Отсутствуют: _В данной ситуации сложно оценить сильные стороны, так как ответ не содержит никакой технической информации\\._

*🚀 Возможности роста*
*Что улучшить:*
• *Эмоциональная реакция* \\- _Может негативно повлиять на рабочие отношения и процесс коммуникации\\._`;

  const result = formatMessage(input);
  assertEquals(result, expected, "Реальный пример не прошел");
});

Deno.test("Тест форматирования ответа LLM", () => {
  const input = `Привет! Отличный вопрос. Ты затронул очень важную тему оптимизации производительности в React. Давай разберем твой ответ шаг за шагом и посмотрим, как можно улучшить решение.

<b>📌 Основные выводы</b>
Ты перечислил много полезных инструментов и библиотек, которые могут пригодиться при разработке React-приложения. Однако, в ответе не хватает конкретики по оптимизации перерисовки списка, из-за которого возникает "мигание". Было бы здорово, если бы ты описал, какие именно техники React можно использовать для решения этой проблемы.

<b>👍 Сильные стороны</b>
• Широкий охват инструментов - Ты знаешь много полезных инструментов для разработки, что говорит о твоем опыте.
• Упоминание библиотек для управления состоянием и работы с данными - Это показывает понимание, что данные нужно эффективно обрабатывать.

<b>🚀 Возможности роста</b>
<b>Что улучшить:</b>
• Отсутствие конкретики по оптимизации перерисовки - Непонятно, как именно ты планируешь бороться с "миганием" при обновлении списка.
• Недостаточно обоснования выбора инструментов - Необходимо объяснить, почему ты выбрал именно эти инструменты и как они помогут решить проблему.`;

  const expected = `Привет\\! Отличный вопрос\\. Ты затронул очень важную тему оптимизации производительности в \`React\`\\. Давай разберем твой ответ шаг за шагом и посмотрим, как можно улучшить решение\\.

*📌 Основные выводы*
Ты перечислил много полезных инструментов и библиотек, которые могут пригодиться при разработке \`React\`\\-приложения\\. Однако, в ответе не хватает конкретики по оптимизации перерисовки списка, из\\-за которого возникает "мигание"\\. Было бы здорово, если бы ты описал, какие именно техники \`React\` можно использовать для решения этой проблемы\\.

*👍 Сильные стороны*
• Широкий охват инструментов \\- Ты знаешь много полезных инструментов для разработки, что говорит о твоем опыте\\.
• Упоминание библиотек для управления состоянием и работы с данными \\- Это показывает понимание, что данные нужно эффективно обрабатывать\\.

*🚀 Возможности роста*
*Что улучшить:*
• Отсутствие конкретики по оптимизации перерисовки \\- Непонятно, как именно ты планируешь бороться с "миганием" при обновлении списка\\.
• Недостаточно обоснования выбора инструментов \\- Необходимо объяснить, почему ты выбрал именно эти инструменты и как они помогут решить проблему\\.`;

  const result = formatMessage(input);
  assertEquals(
    result,
    expected,
    "Форматирование ответа LLM не соответствует ожидаемому"
  );
});

Deno.test("Тест сложных ответов LLM - Код с вложенным форматированием", () => {
  const input = `Давайте рассмотрим несколько подходов к оптимизации в \`React\`:

<b>1. Оптимизация рендеринга</b>
• Используйте <code>React.memo()</code> для предотвращения лишних рендеров:
\`\`\`javascript
const MyComponent = React.memo(({ data }) => {
  // Компонент будет перерендерен только при изменении props
  return <div>{data.map(item => (
    <span key={item.id}>{item.name}</span>
  ))}</div>;
});
\`\`\`

<b>2. Управление состоянием</b>
• Используйте <code>useMemo</code> для кэширования результатов:
\`\`\`javascript
const memoizedValue = useMemo(() => {
  return expensiveCalculation(data);
}, [data]);
\`\`\`

<i>Важно помнить</i>: не злоупотребляйте мемоизацией - она тоже требует ресурсов.`;

  const expected = `Давайте рассмотрим несколько подходов к оптимизации в \`React\`:

*1\\. Оптимизация рендеринга*
• Используйте \`React.memo()\` для предотвращения лишних рендеров:
\`\`\`javascript
const MyComponent = React.memo(({ data }) => {
  // Компонент будет перерендерен только при изменении props
  return <div>{data.map(item => (
    <span key={item.id}>{item.name}</span>
  ))}</div>;
});
\`\`\`

*2\\. Управление состоянием*
• Используйте \`useMemo\` для кэширования результатов:
\`\`\`javascript
const memoizedValue = useMemo(() => {
  return expensiveCalculation(data);
}, [data]);
\`\`\`

_Важно помнить_: не злоупотребляйте мемоизацией \\- она тоже требует ресурсов\\.`;

  const result = formatMessage(input);
  assertEquals(
    result,
    expected,
    "Форматирование кода с вложенным форматированием не соответствует ожидаемому"
  );
});

Deno.test("Тест сложных ответов LLM - Списки и маркеры", () => {
  const input = `<b>📌 Основные проблемы производительности:</b>

1. <i>Излишние ререндеры</i>
   • Когда: при обновлении состояния
   • Решение: использовать <code>React.memo()</code>

2. <i>Тяжелые вычисления</i>
   • Когда: при обработке больших массивов
   • Решение: применять <code>useMemo()</code>

<b>🚀 Рекомендации по оптимизации:</b>
• Профилирование через <code>React DevTools</code>
• Анализ через <code>Performance</code> вкладку
• Использование <code>React.lazy()</code> для разделения кода`;

  const expected = `*📌 Основные проблемы производительности:*

1\\. _Излишние ререндеры_
   • Когда: при обновлении состояния
   • Решение: использовать \`React.memo()\`

2\\. _Тяжелые вычисления_
   • Когда: при обработке больших массивов
   • Решение: применять \`useMemo()\`

*🚀 Рекомендации по оптимизации:*
• Профилирование через \`React DevTools\`
• Анализ через \`Performance\` вкладку
• Использование \`React.lazy()\` для разделения кода`;

  const result = formatMessage(input);
  assertEquals(
    result,
    expected,
    "Форматирование списков и маркеров не соответствует ожидаемому"
  );
});

Deno.test(
  "Тест сложных ответов LLM - Смешанное форматирование и специальные символы",
  () => {
    const input = `<b>Анализ производительности React-приложения (v18.0+)</b>

<i>Шаг 1:</i> Используйте <code>React.Profiler</code> для замера времени рендеринга:
\`\`\`jsx
<Profiler id="MyComponent" onRender={(phase, actualTime) => {
  console.log(\`Компонент MyComponent затратил: \${actualTime}ms\`);
}}>
  <MyComponent />
</Profiler>
\`\`\`

<i>Шаг 2:</i> Проверьте метрики в Chrome DevTools:
• FCP (First Contentful Paint) < 2s
• LCP (Largest Contentful Paint) < 2.5s
• FID (First Input Delay) < 100ms

<b>❗️ Важные замечания:</b>
• Не используйте <code>index</code> как key в списках
• Применяйте <code>{...props}</code> с осторожностью
• Следите за размером бандла (max: ~100kb)`;

    const expected = `*Анализ производительности \`React\`\\-приложения \\(v18\\.0\\+\\)*

_Шаг 1:_ Используйте \`React.Profiler\` для замера времени рендеринга:
\`\`\`jsx
<Profiler id="MyComponent" onRender={(phase, actualTime) => {
  console.log(\`Компонент MyComponent затратил: \${actualTime}ms\`);
}}>
  <MyComponent />
</Profiler>
\`\`\`

_Шаг 2:_ Проверьте метрики в Chrome DevTools:
• FCP \\(First Contentful Paint\\) \\< 2s
• LCP \\(Largest Contentful Paint\\) \\< 2\\.5s
• FID \\(First Input Delay\\) \\< 100ms

*❗️ Важные замечания:*
• Не используйте \`index\` как key в списках
• Применяйте \`{...props}\` с осторожностью
• Следите за размером бандла \\(max: \\~100kb\\)`;

    const result = formatMessage(input);
    assertEquals(
      result,
      expected,
      "Форматирование смешанного текста со специальными символами не соответствует ожидаемому"
    );
  }
);
